package {
	// Imports
	import flash.display.MovieClip;
	import fl.controls.Button;
	
    public class creerClasse extends MovieClip {
        // Variables
		
        // Constructeur
        public function creerClasse():void{
			addChild(new Button());
        }

        // Fonctions

    } // Fin de classe
} // Fin de package